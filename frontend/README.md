# Frontend

This directory will host the Flutter Web dashboard for F-Build.

The first frontend feature branch will initialize the Flutter project and build
the project list / build queue pages against the backend API.

